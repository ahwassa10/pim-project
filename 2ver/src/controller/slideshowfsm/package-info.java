/**
 * Holds the implementation of the slideshow.
 * Currently, this package only contains one class, but this class implements
 * a finite state machine that represents the slideshow. The slideshow acts as a
 * broker between the logical model and the visual representation of a slideshow.
 */
package controller.slideshowfsm;