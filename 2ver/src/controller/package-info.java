/**
 * Holds the implementations of the program, photo, and slideshow finite state
 * machines, as well as the launch point into the program (Photos.java).
 * 1) program fsm: controls the main program (user and group functionality).
 * 2) photo fsm: a new window is created when the user selects a photo.
 * 3) slideshow fsm: a new window is created to display a slideshow of the group.
 */
package controller;